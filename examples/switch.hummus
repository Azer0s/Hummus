(def switch (macro |value| |statements|
  (map statements (fn statement
    (' :if (' :call := value (nth 0 statament))
      (nth 1 statament)
    )
  ))
))

(def x (in))

(switch x
  ("Hello" (out "Hi!"))
  ("Bye" (out "See ya!"))
)
